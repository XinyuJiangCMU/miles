"""AMD-specific DSv4 kernel implementations (Triton/Liger).

Physically isolated from the NV mainline (miles_plugins/models/deepseek_v4) so
platform divergence lives here, not as scattered if-hip in the model code.
"""
